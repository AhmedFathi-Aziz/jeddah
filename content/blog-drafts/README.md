# مسودات المدونة (من D1)

يُملأ هذا المجلد تلقائياً عند تشغيل `npm run articles:export-from-d1` للمقالات غير المنشورة (`published = 0`).

الواجهة **لا** تقرأ من هنا — فقط `content/blog/`. انقل أي ملف إلى `content/blog/` بعد ضبط `published: true` في الـ frontmatter إن أردت نشره.
