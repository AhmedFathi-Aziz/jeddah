import { absUrl } from "@/lib/site-config";

/** معرفات ثابتة لربط كيانات schema.org عبر الصفحات */
export const SCHEMA_ORGANIZATION_ID = `${absUrl("/")}#organization`;
export const SCHEMA_WEBSITE_ID = `${absUrl("/")}#website`;
export const SCHEMA_LOCAL_BUSINESS_ID = `${absUrl("/")}#localbusiness`;
/** صفحة الأسئلة الشائعة (الصفحة الرئيسية فقط) */
export const SCHEMA_FAQ_PAGE_ID = `${absUrl("/")}#faqpage`;
