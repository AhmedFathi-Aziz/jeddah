import type { NewsItem } from "./types";

/**
 * احتياطي قديم — الأخبار تُدار من ملفات Markdown في content/news/
 * (راجع content/news/README.md). أضف هنا فقط إن لم تستخدم ملفاً.
 */
export const newsItems: NewsItem[] = [];
